---
layout: layout.liquid
title: Orion Leidl Wilson
permalink: "/index.html"
description: "Welcome to my personal website! Here you will find information about me, my projects"
---

<main class="container">
  <section>
    <h1>Orion Leidl Wilson</h1>
    <p>
      Welcome to my personal website! Here you will find information about me,
      my projects, and my blog posts. Feel free to explore and connect with me!
      This site is very much a work in progress, and will be evolving over the
      summer.
    </p>
  </section>
  <section>
    <h2>Latest Posts</h2>
    <div class="flex three">
      {% for post in collections.post | reverse | slice(0, 3) %}
      <article class="card blog-preview">
        <header>
          <h3 style="margin-bottom: 0.5em">
            <a href="{{ post.url }}">{{ post.data.title }}</a>
          </h3>
          <small>{{ post.date | date("yyyy-MM-dd") }}</small>
        </header>
        {% if post.data.image %}
        <a href="{{ post.url }}">
          <img
            src="{{ post.data.image }}"
            alt="{{ post.data.title }}"
            style="
              max-width: 100%;
              height: auto;
              display: block;
              margin: 0 auto 1em auto;
            "
          />
        </a>
        {% endif %}
        <p>
          {% if post.data.description %}
          {{ post.data.description }}
          {% else %}
          {{ post.templateContent | striptags | truncate(200, true, "…") }}
          {% endif %}
        </p>
      </article>
      {% endfor %}
    </div>
  </section>
</main>
